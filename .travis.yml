lenguage: node_js

node_js:
  - "node"

script:
  - bash deploy.sh
cache:
  directories:
    - node_modules

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  on:
    branch: main
